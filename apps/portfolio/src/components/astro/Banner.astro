---

interface Props {
  title: string;
  className: string;
}

const { title, className } = Astro.props;
---

<div class={`absolute w-screen h-6 top-0 flex justify-center items-center ${className}`}>
  <span class="font-bold text-white text-md text-center">{ title }</span>
</div>